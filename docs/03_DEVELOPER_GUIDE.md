Project Principles

Never bypass the Planner.

Never call an agent directly from the UI.

Always register new agents through AgentRegistry.

Memory must never execute code.

Discovery must never auto-install agents.

Every new capability must be evaluated.

Every feature must preserve modularity.